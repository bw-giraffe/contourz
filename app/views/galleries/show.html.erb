<h1><%= @gallery.theme %></h1> 
<div class = "row">
	<div id = "galleryPic">
		<% @photos.each do |p| %>
		<div class= "col-sm-3 col-sm-offset-1"><%= link_to image_tag(p.url, id: "reference"), renditions_path(p.id) %></div>
			<br>
		<% end %> 
	</div>
</div>


